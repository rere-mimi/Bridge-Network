/**
 * Legacy Appendix B catalogue retained for reference.
 * Live inventory now uses Appendix C — see `elementSchedule.ts`.
 */
export {
  STANDARD_ELEMENTS as STANDARD_COMPONENTS,
  elementsForFamily as componentsForFamily,
  groupLabel,
  type ElementGroup as ComponentGroup,
  type StructureFamily,
  type QuantityUnit,
  type StandardElement as StandardComponent,
} from './elementSchedule'
